# PyTorch with CUDA 12.1 - compatible versions
# torch 2.6.0+ is required for torch.int1 support (needed by torchao)
# 
# Installation instructions:
# Step 1: Install PyTorch from CUDA 12.1 index:
#   pip install torch==2.6.0 torchvision==0.21.0 torchaudio==2.6.0 --index-url https://download.pytorch.org/whl/cu121
# Step 2: Install other dependencies:
#   pip install -r requirements.txt

# Core dependencies with compatible versions
torchao==0.12.0
diffusers
pillow
